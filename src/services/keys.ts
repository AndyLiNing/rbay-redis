export const USERNAMES_KEY = 'usernamekey';
export const pageCacheKey = (id: string) => `pagecache#${id}`;

export const userKey = (id: string) => `users#${id}`

export const sessionKey = (id: string) => `sessions${id}`
